<%= form_with(model: scrum_project, local: true) do |form| %>
  <% if scrum_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scrum_project.errors.count, "error") %> prohibited this scrum_project from being saved:</h2>

      <ul>
      <% scrum_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
